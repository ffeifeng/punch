# 本地开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/punch?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 123456

# 本地开发环境的日志配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # 本地开发启用SQL日志输出
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# Redis配置（如果使用）
# spring:
#   redis:
#     host: localhost
#     port: 6379
#     password: 
#     database: 0
