<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.punch.mapper.CheckinCompleteRewardMapper">

    <insert id="insert" parameterType="com.punch.entity.CheckinCompleteReward" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO checkin_complete_reward (student_id, template_id, reward_date, lottery_count, create_time)
        VALUES (#{studentId}, #{templateId}, #{rewardDate}, #{lotteryCount}, NOW())
    </insert>

    <select id="countByStudentTemplateDate" resultType="int">
        SELECT COUNT(1) FROM checkin_complete_reward
        WHERE student_id = #{studentId}
          AND template_id = #{templateId}
          AND reward_date = DATE(#{rewardDate})
    </select>

    <select id="selectByStudentTemplateDate" resultType="com.punch.entity.CheckinCompleteReward">
        SELECT * FROM checkin_complete_reward
        WHERE student_id = #{studentId}
          AND template_id = #{templateId}
          AND reward_date = DATE(#{rewardDate})
        LIMIT 1
    </select>

    <delete id="deleteByStudentTemplateDate">
        DELETE FROM checkin_complete_reward
        WHERE student_id = #{studentId}
          AND template_id = #{templateId}
          AND reward_date = DATE(#{rewardDate})
    </delete>

</mapper>
